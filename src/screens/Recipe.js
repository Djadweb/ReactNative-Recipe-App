import React from 'react'
import { View, Text, FlatList } from 'react-native'

function Recipe({ recipe }) {
  return (
    <View>
        {/* <View style={styles.recipe__image}>            
            <Image             
            style={styles.image}
            source={recipe.image}
            />
        </View>
        <Text>{recipe.name}</Text>
        <Text>{recipe.ingredients}</Text> */}
    </View>
  )
}

export default Recipe